// Write a C# program to check whether an alphabet is vowel or consonant using switch 
//statement.

using System;

namespace Ishma_dot_net_practical_1
{
    internal class IshmaLab1Qn10
    {
        
        /*
        static void Main(string[] args)
        {
            Console.WriteLine("Enter an alphabet: ");
         
            char alp = char.ToLower(Console.ReadLine()[0]);

            switch (alp)
            {
                case 'a':
                case 'e':
                case 'i':
                case 'o':
                case 'u':
                    Console.WriteLine($"{alp} is vowel");
                    break;

                default:
                    if (char.IsLetter(alp))
                    {
                        Console.WriteLine($"{alp} is consonant");
                    }
                    else
                    {
                        Console.WriteLine("Not a valid input");
                    }
                    break;
   
            }
            Console.ReadLine();
        }
        */
        
    }
}
