//Write a C# program to print the result of the specified operations- Take two input 
//number and one operator from user. 

using System;

namespace Ishma_dot_net_practical_1
{
    internal class Lab1Qn5
    {
        /*
        static void Main(string[] args)
        {
            Console.WriteLine("Enter first number: ");
            double a = double.Parse(Console.ReadLine());

            Console.WriteLine("Enter second number: ");
            double b = double.Parse(Console.ReadLine());

            Console.WriteLine("Enter operator (+,-,*,/,%) :");
            char ope = char.Parse(Console.ReadLine());

            double ans;

            
            switch (ope)
            {
                case '+':
                    ans = a + b;
                    Console.WriteLine("Sum of {0} and {1} is {2} ", a, b, ans);
                    break;

                case '-':
                    ans = a - b;
                    Console.WriteLine("Difference betn {0} and {1} is {2} ", a, b, ans);
                    break;

                case '*':
                    ans = a * b;
                    Console.WriteLine("Multiplication of {0} and {1} is {2} ", a, b, ans);
                    break;

                case '%':
                    ans = a % b;
                    Console.WriteLine("Modulus of {0} and {1} is {2} ", a, b, ans);
                    break;

                case '/':
                    if(b == 0)
                    {
                        Console.WriteLine("Error: Denominator cannot be 0");
                    }
                    else {
                        ans = a / b;
                        Console.WriteLine("{0} divided by {1} is {2} ", a, b, ans);
                    }
                    break;

                default:
                    Console.WriteLine("Invalid Operator");
                    break;
            }
            Console.ReadLine();
        }
        */
    }
}
