//Write a C# program to swap two numbers entered by users.

using System;

namespace Ishma_dot_net_practical_1
{
    internal class IshmaLab1Qn6
    {
        /*
        static void Main(string[] args)
        {
            Console.WriteLine("Enter a:");
            int a = int.Parse(Console.ReadLine());

            Console.WriteLine("Enter b: ");
            int b = int.Parse(Console.ReadLine());

            Console.WriteLine("Before swapping Value of 'a' is {0} and 'b' is {1}  ", a, b);
            int temp;
            temp = a;
            a = b;
            b = temp;

            Console.WriteLine("After swapping Value of 'a' is {0} and 'b' is {1} ", a, b);
            Console.ReadLine() ;
        }
        */
    }
}
