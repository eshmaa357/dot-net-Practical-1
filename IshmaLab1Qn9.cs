// Write a C# program to input number from user and find odd even accordingly. 

using System;

namespace Ishma_dot_net_practical_1
{
    internal class IshmaLab1Qn9
    {
        /*
        static void Main(string[] args)
        {
            while (true)
            {
                Console.WriteLine("Enter any input or type 'exit' to quit");
                string input = Console.ReadLine();

                if ((input.ToLower() == "exit")){
                    break;
                }

                if(double.TryParse(input, out double num))
                {
                   if(num % 2 == 0)
                    {
                        Console.WriteLine($"{num} is even number");
                    }
                    else
                    {
                        Console.WriteLine($"{num} is odd number");
                    }
                }
                else
                {
                    Console.WriteLine("Invalid input. Try again");
                }
                
                 
            }
            Console.WriteLine("Program Execute");
            Console.ReadLine();
        }
        */
    }
}
